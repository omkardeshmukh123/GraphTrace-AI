# GraphTrace AI — Unified Requirements
# Covers: M1 (Artifact Intelligence) + M2 (Knowledge Graph) + M3 (Backend API)
#
# Install: pip install -r requirements.txt
# Dev:     pip install -r requirements.txt -r requirements-dev.txt

# ─── Web framework (M3 uses 0.135.2; using compatible newer version) ─────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# ─── Data validation & settings ──────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# ─── Neo4j driver (M3 requires >=5.19,<7 — our 5.24.0 satisfies this) ───────
neo4j==5.24.0

# ─── HTTP client (used in tests) ─────────────────────────────────────────────
httpx==0.27.2
